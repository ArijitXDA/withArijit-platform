# Ops log — 2026-06-11 (applied directly to prod via Supabase MCP, not via migrations)

These are operational/data changes that live in prod but are NOT migration files
(one-off data ops + cron config). Recorded here so the repo tells the truth.

## Cron jobs added (pg_cron)
| job | schedule | command |
|---|---|---|
| `lifecycle-partner-no-referral-tick` (jobid 31) | `0 4 * * *` | `SELECT lifecycle_emit_partner_no_referral_tick();` |
| `lifecycle-membership-upsell-tick` (jobid 32) | `30 4 * * *` | `SELECT lifecycle_emit_membership_upsell_tick();` |
| `lifecycle-enrol-abandoned-tick` (jobid 33) | `0 * * * *` | `SELECT lifecycle_emit_enrol_abandoned_tick();` |
| `lifecycle-hot-lead-alert-tick` (jobid 34) | `15 * * * *` | `SELECT lifecycle_emit_hot_lead_attended_tick();` |

## Cron jobs removed
- `send-enrol-nudge-drip-email-daily` (was jobid 17) — UNSCHEDULED 2026-06-11.
  The legacy Day-3/7/14 post-webinar enrol email drip double-messaged attendees
  against lifecycle S6/S8 (conflicting "last call" claims) and its enrol links
  dropped partner attribution. Replaced by the S6/S8 conversion-drip steps
  (see migration 20260611105916). The edge function
  `send-enrol-nudge-drip-email` still exists but nothing calls it.

## Data ops (already in prod; not reproducible-by-migration by design)
- **12 WhatsApp lifecycle templates** inserted (2026-06-11, pre-noon): the
  `wa_p7_*`, `wa_s4_welcome_wa_v1`, `wa_p4_pulse_wa_v1`, `wa_e1/e2_*`,
  `wa_s9_referral_wa_v1`, `wa_s10_*`, `wa_hotlead_partner_alert_v1`,
  `wa_m1_*` rows. AiSensy campaign names = `lifecycle_<key-without-wa_>`;
  all 12 campaigns Meta-approved + Live (operator-confirmed; `lifecycle_p7_first_share_v1`
  delivery-tested to the founder's number).
- **S7 extension** (2026-06-07): steps 4/5/6 added to
  `s7_free_webinar_noshow_recovery` — day-14 email `em_s7_next_session_v1`,
  day-28 WA `wa_s7_we_missed_you_v1`, day-45 email `em_s7_next_session_v1`,
  all `absolute_anchor='webinar_date'`.
- **Conversion-nudge WA activation** (2026-06-11): `wa_s6/s8_nudge2..5_v1`
  flipped `is_active=true` after the operator confirmed the `nudge_2..5`
  AiSensy campaigns are Live (the port migration creates them inactive).
- **student_reminder_templates repoints** (2026-06-11): the three broken
  `*_v2` AiSensy campaign names (`webinar_countdown_reminder_v2`,
  `webinar_feedback_request_v2`, `webinar_noshow_reengage_v2` — countdown_v2
  had 37 fails / 0 successes) repointed to the proven v1 campaign names.
  `send-student-reminder` picks param shape by campaign name, so this is safe.
  Reversible if the v2 templates ever get Meta-approved.
- **Re-enrolment rescue wave** (2026-06-07): 33 exited p1 enrolments
  reactivated; ~417 students enrolled into s6/x1, staggered, context tagged
  `reengagement_wave: 2026-06-07`.
- **Teams-link fix** (2026-06-08): `awa_webinar_sessions.ms_teams_link`
  backfilled from `meeting_link` for scheduled partner sessions.

## Edge functions
- `lifecycle-dispatcher` deployed **v10** 2026-06-11 (source mirrored at
  `supabase/functions/lifecycle-dispatcher/index.ts`). Deploy from that file
  ONLY — never from an older checkout.
